<div id="<%= dom_id brewery %>">
  <h2> 
    <strong>Name:</strong>
    <%= link_to brewery.name, brewery %>
  </h2>
  <p><i>Established in <%=brewery.year %></i></p>
  <p>
   <% brewery.beers.each do |beer| %>
     <%= link_to beer.name, beer %>
   <% end %>

  </p>
  <% if brewery.ratings.empty? %>
  <% else %>
  This brewery's beers have <%= pluralize(brewery.ratings.count, 'rating') %> with an average rating of <%= brewery.average_rating %>
  <% end %>

</div>
